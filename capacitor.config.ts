import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.wordscramblerace',
  appName: 'Word Scramble Race',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
